#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int H;
    cin >> H;
    vector<int> parent(H*H+2, -1);
    vector<vector<bool> > maze(H+2, vector<bool>(H+2, 0));
    return 0;
}
